import { CookieJar } from "./types";
/**
 * Decode response body based on content encoding
 */
export declare function decodeBody(buffer: Buffer, encoding?: string): string;
/**
 * Fetch text from URL with retries and rate limit handling
 */
export declare function fetchText(url: string, options?: {
    jar?: CookieJar;
    maxRetries?: number;
    retryCount?: number;
    proxy?: string;
    noCheckCertificate?: boolean;
}): Promise<string>;
/**
 * Extract filename from Content-Disposition header
 */
export declare function extractFilenameFromDisposition(disposition: string): string | null;
/**
 * Sanitize filename for filesystem
 */
export declare function sanitizeFileName(name: string, fallback: string): string;
/**
 * Ensure unique filename in directory
 */
export declare function ensureUniqueName(dir: string, desiredName: string): string;
/**
 * Sleep for specified milliseconds
 */
export declare function sleep(ms: number): Promise<void>;
/**
 * Format bytes to human readable string
 */
export declare function formatBytes(bytes: number): string;
//# sourceMappingURL=utils.d.ts.map